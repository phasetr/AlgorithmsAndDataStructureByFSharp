# ## P.17 Challenge 9: Initials Only
# 3 つの文字列引数を持つ関数を書け.
# 特にそれぞれの文字列の先頭の文字を取って結合させてから返す関数とせよ.
function initials_only(first, middle, last)
  first[begin] * middle[begin] * last[begin]
end
println(initials_only("abc", "def", "ghi"))
